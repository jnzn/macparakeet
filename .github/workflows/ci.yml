name: CI

on:
  push:
  pull_request:

jobs:
  swift-test:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Swift Test
        run: swift test --parallel

